{
  "name": "tasks-app",
  "version": "1.0.0",
  "description": "Google Calendar task rollover — runs nightly on AWS Lambda",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/local.js",
    "test": "node src/local.js"
  },
  "devDependencies": {
    "dotenv": "^16.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.0.0",
    "googleapis": "^144.0.0",
    "luxon": "^3.0.0"
  }
}
